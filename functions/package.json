{
  "name": "coffee-dashboard-functions",
  "description": "Cloud Function that turns the dashboard's computed statistics into AI-written recommendations (Claude Haiku).",
  "version": "1.0.0",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0"
  }
}
